import 'package:flutter/material.dart';

class ViewRequestsView extends StatelessWidget {
  const ViewRequestsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("View requests")),
    );
  }
}
