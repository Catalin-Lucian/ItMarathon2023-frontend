import 'package:flutter/material.dart';

class SendRequestView extends StatelessWidget {
  const SendRequestView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Send request")),
    );
  }
}
